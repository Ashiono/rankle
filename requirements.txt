# Rankle - Web Infrastructure Reconnaissance Tool
# Requirements file for Python dependencies
# Compatible with Python 3.11+ (tested up to 3.14)
# Last updated: November 2025

# ============================================================================
# REQUIRED DEPENDENCIES
# ============================================================================
# These are mandatory for Rankle to function

requests>=2.31.0
# HTTP library for making web requests
# Replaces: curl commands

dnspython>=2.4.0
# DNS toolkit for Python
# Replaces: dig commands

beautifulsoup4>=4.12.0
# HTML parsing library
# Used for: Technology detection and web scraping

# ============================================================================
# OPTIONAL DEPENDENCIES
# ============================================================================
# These enhance functionality but are not strictly required
# Install with: pip install rankle[extended]

python-whois>=0.9.0
# WHOIS protocol client
# Replaces: whois command
# Provides: Domain registration information

ipwhois>=1.2.0
# IP address WHOIS and ASN lookup
# Provides: Enhanced IP geolocation and network information

# ============================================================================
# INSTALLATION INSTRUCTIONS (2025)
# ============================================================================
#
# Recommended: Use pyproject.toml (modern way)
#   pip install -e .
#   pip install -e ".[dev]"      # With dev dependencies
#   pip install -e ".[extended]" # With optional dependencies
#
# Legacy: Using requirements.txt
#   pip install -r requirements.txt
#
# Create virtual environment (recommended):
#   python3.14 -m venv venv
#   source venv/bin/activate  # On Windows: venv\Scripts\activate
#   pip install -e .
#
# ============================================================================
